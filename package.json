{
  "name": "kiffane-cod-funnel",
  "private": true,
  "version": "1.0.0",
  "description": "Tunnel de vente E-commerce COD (Cash On Delivery) pour kiffane.com hébergé sur Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
